# scripts/scan

Shell scanners for FLEX/OSPC inventory and boot state.

Destined for this folder:
`flexvmscan.sh`, `ospcscan.sh`, `vmbootscan.sh`,
`test_flex_vms.sh`, `test_migrated_vms.sh`, `test_account_active_dependency_scanner.sh`

> Placeholder. Files are still in the repo root; nothing has been moved yet.
> Moving them requires updating the call sites in `workflow_dashboard/app.py`.
