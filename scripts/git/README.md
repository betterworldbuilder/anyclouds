# scripts/git

Repo sync helpers for the betterworld and rackerlabs remotes.

Destined for this folder:
`gitbwb.sh`, `gitpullbwb.sh`, `gitpushbwb.sh`, `gitpullrack.sh`,
`pushrack.sh`, `remote_push_home_from_origin.sh`

**Wiring note:** these assume they run from the repo root; they need a
`cd "$(dirname "$0")/../.."` guard once moved.

> Placeholder. Files are still in the repo root; nothing has been moved yet.
> Moving them requires updating the call sites in `workflow_dashboard/app.py`.
