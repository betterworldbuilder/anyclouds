# assets/media

Audio/image assets used by the dashboard UI.

Destined for this folder:
`eaglelandeshort.mp3`, `moonsurf.jpg`, `gemini-svg.svg`

**Wiring note:** `eaglelandeshort.mp3` and `moonsurf.jpg` are referenced by
`workflow_dashboard/templates/image_migrator.html` — update those `src` paths on move.

> Placeholder. Files are still in the repo root; nothing has been moved yet.
> Moving them requires updating the call sites in `workflow_dashboard/app.py`.
